name: Keep Railway Alive

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Railway URL
        run: curl -s -o /dev/null -w "%{http_code}" https://dine-in-system-production.up.railway.app
